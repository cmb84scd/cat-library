.yarn_job:
  image: node:18-alpine
  before_script:
    - yarn

stages:
  - test

test:
  stage: test
  extends: .yarn_job
  script: yarn test

lint:
  stage: test
  extends: .yarn_job
  script: yarn lint

types:
  stage: test
  extends: .yarn_job
  script: yarn types
